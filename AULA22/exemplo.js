const io = require("../Io/io") // Importando a biblioteca

// Forma mais utilizada para a repetição. Lembrando que quando a condição é fixa, usa-se o "for", caso a condição seja uma variável utiliza-se mais o while.

for (let i = 0; i < 100; i++) {
    io.write("Brasil") // 'if e else' funciona normal dentro das chaves.
}